PORT=5000
JWT_SECRET=replace-with-a-long-random-string

# Optional. If unset, booking/waitlist confirmations are simulated via
# console.log instead of sending real email.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Burger Bonanza" <reservations@burgerbonanza.com>
